const PI = 3.14;

function sumar(x, y) {
    return x + y;
}

const restar = (x, y) => {
    return x - y;
};

function multiplicar(a, b) {
    return a * b;
}

const dividir = (a, b) => {
    return a / b;
};

const numerosStrings = ["dos", "cuatro", "ocho", "diez"];

export { PI, sumar, restar, multiplicar, dividir, numerosStrings };
